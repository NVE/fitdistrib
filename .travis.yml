language: r
cache: packages

r_packages:
  - covr

r_github_packages:
  - NVE/FlomKart_ShinyApp

env:
  global:
    - GITHUB_REPO: "NVE/fitdistrib"
    - GITHUB_PAT

before_script:
  - Rscript -e 'library(covr); codecov()'

script:
  - chmod +x ./_deploy.sh
  - ./_deploy.sh  

